import { Button, Text } from "@chakra-ui/react";

function App() {
  return (
    <div>
      <Button colorScheme="teal" mt={4} >
        Click Me
      </Button>
    </div>
  );
}

export default App;
